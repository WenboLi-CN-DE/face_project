# 核心依赖 - InsightFace
insightface>=0.7.3
opencv-python>=4.8.0
numpy>=1.24.0
onnxruntime>=1.16.0
pillow>=10.0.0

# 活体检测 - MediaPipe
mediapipe>=0.10.0

# Web API 框架 - FastAPI
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6

# 异步 HTTP 客户端 - 用于回调
httpx>=0.25.0

# 生产服务器（可选，Docker 部署使用）
gunicorn>=21.0.0
gevent>=23.0.0

# 开发依赖
pytest>=7.4.0
pytest-cov>=4.1.0
pytest-asyncio>=0.21.0
black>=23.0.0
flake8>=6.0.0
mypy>=1.5.0

# 可选：GPU 支持（与 onnxruntime 二选一）
# onnxruntime-gpu>=1.16.0

